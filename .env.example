# 本番（EC2）で RDS に接続するための環境変数のサンプル。
# このファイルをコピーして .env を作り、実際の値を入れる:
#   cp .env.example .env
# .env 自体は git にコミットされない（.gitignore 済み）。
DB_HOST=your-db.xxxxxxxx.ap-northeast-1.rds.amazonaws.com
DB_USERNAME=postgres
DB_PASSWORD=your-password
